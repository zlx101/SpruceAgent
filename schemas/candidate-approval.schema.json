{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://spruceagent.dev/schemas/candidate-approval.schema.json",
  "title": "SpruceAgent Candidate Approval",
  "type": "object",
  "required": ["version", "status", "resultCount", "summary", "results", "limits"],
  "additionalProperties": false,
  "properties": {
    "version": { "type": "string" },
    "status": {
      "type": "string",
      "enum": ["completed", "requires_approval", "completed_with_blockers"]
    },
    "resultCount": { "type": "integer", "minimum": 0 },
    "summary": {
      "type": "object",
      "additionalProperties": { "type": "integer", "minimum": 0 }
    },
    "results": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["stepId", "status"],
        "additionalProperties": true,
        "properties": {
          "stepId": { "type": "string" },
          "status": {
            "type": "string",
            "enum": ["approval_created", "approval_reused", "blocked", "skipped"]
          },
          "promotionStatus": {
            "type": "string",
            "enum": ["ready", "requires_approval", "blocked", "not_promotable"]
          },
          "reason": { "type": "string" },
          "approval": { "type": "object" }
        }
      }
    },
    "limits": {
      "type": "array",
      "items": { "type": "string" }
    }
  }
}
