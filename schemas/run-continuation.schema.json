{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://spruceagent.dev/schemas/run-continuation.schema.json",
  "title": "SpruceAgent Run Continuation",
  "type": "object",
  "required": ["version", "status", "traceId", "sourceRun", "resultCount", "summary", "results", "limits"],
  "additionalProperties": false,
  "properties": {
    "version": { "type": "string" },
    "status": {
      "type": "string",
      "enum": ["completed", "requires_approval", "completed_with_blockers"]
    },
    "traceId": { "type": "string", "pattern": "^trace_[a-z0-9_]+$" },
    "sourceRun": {
      "type": "object",
      "additionalProperties": true
    },
    "resultCount": { "type": "integer", "minimum": 0 },
    "summary": {
      "type": "object",
      "additionalProperties": { "type": "integer", "minimum": 0 }
    },
    "results": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["stepId", "status"],
        "additionalProperties": true,
        "properties": {
          "stepId": { "type": "string" },
          "status": {
            "type": "string",
            "enum": ["succeeded", "failed", "blocked", "requires_approval", "skipped"]
          },
          "promotionStatus": {
            "type": "string",
            "enum": ["ready", "requires_approval", "blocked", "not_promotable"]
          },
          "approvalId": { "type": "string" },
          "reason": { "type": "string" }
        }
      }
    },
    "limits": {
      "type": "array",
      "items": { "type": "string" }
    }
  }
}
